
import 'package:flutter/material.dart';

class PrataPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Cavaleiros de Prata"),
      ),
    );
  }
}
